# Run report — 2026-07-24

## Scoring

All backlog items and their ROI scores this run (formula per `CLAUDE.md`,
`* 1.5` continuation bonus applied to `in-progress`):

| id | status | impact×confidence×priority/effort | score |
|---|---|---|---|
| `ai-monetization-overnight-automation` | in-progress | 100/5 = 20 | **30** |
| `claude-code-productivity-tools` | in-progress | 80/4 = 20 | **30** |
| `free-ai-learning-resources` | in-progress | 80/5 = 16 | 24 |
| `gumroad-venture-factory-template` | backlog | 100/4 = 25 | 25 |
| `ai-coding-freelance-portfolio-kit` | backlog | 36/3 = 12 | 12 |
| `github-to-claude-code-launcher-extension` | backlog | 27/3 = 9 | 9 |

`ai-monetization-overnight-automation` and `claude-code-productivity-tools`
tied exactly at 30 (same tie as the previous run, which picked the former).
Picked `claude-code-productivity-tools` this time: its checkpoint had four
concrete, closeable next-steps (resolve a naming collision, verify
disputed star counts, attempt Reddit/X practitioner mining, evaluate a
specific rate-limit mechanism) rather than open-ended breadth expansion —
a better fit for reaching a genuine `done` state in one session, which
the finish-over-novelty bias in `POLICY.md` favors over starting fresh
breadth on the sibling file.

## Branch note

This session's designated branch (`claude/youthful-babbage-3gh4ut`) came
in already fast-forwarded to `main` — both prior runs' PRs (#1, #2) had
already merged. Treated as a fresh start per the standing branch
instructions; no history was discarded, the branch simply had nothing
ahead of `main` to begin with.

## Tasks touched

Worked `backlog/research/claude-code-productivity-tools.md` (second
research pass) and closed it out to `status: done`.

Resolved all four queued next-steps:

1. **Headroom naming collision** — pass 1 found four unrelated repos named
   "headroom"; this pass found a **fifth** (`headroomlabs-ai/headroom`,
   ~62k stars, a token-compression library/proxy/MCP server) that pass 1
   missed and that turns out to be far more popular than the other four
   combined. Skillsllm.com's previously-doubted "60.4k★" figure for
   "headroom" was actually citing this repo, not the quota-tracker one
   pass 1 assumed — a mis-attribution, now corrected. Added an explicit
   recommendation split: `headroomlabs-ai/headroom` for usage-extension
   (matches the user's original "become a very powerful usage" framing),
   `allandecastro/headroom` for quota-visibility across Claude/Codex/Copilot
   with server-verified numbers.
2. **Disputed star counts** — verified graphify (~94-94.8k), ponytail
   (~88k), codeburn (8,857), and the newly-found headroomlabs-ai/headroom
   (~62k) against independent sources (trendshift.io, star-history.com,
   direct repo fetches). Pass 1's skepticism toward `skillsllm.com`'s
   numbers turned out to be **wrong** — its figures were all within a few
   percent of live counts, just lagging by days/weeks on fast-growing
   repos, not inflated. Corrected that framing throughout the file.
3. **Reddit/X practitioner-retention mining** — attempted, came back thin
   again, same indexing gap as every prior pass in this project. Recorded
   as a confirmed-negative result rather than left open, since repeating
   the same search approach is unlikely to change the outcome.
4. **ccusage's rate-limit mechanism** — confirmed real via a direct fetch
   of Anthropic's own `code.claude.com/docs/en/statusline` (not a
   secondary blog, unlike the already-debunked `/cost` claim from pass 1).
   Claude Code's `statusLine` feature does pass a genuine `rate_limits`
   object (5-hour/7-day windows) to a configured script on stdin, for
   Pro/Max subscribers. However, this is gated on an *interactive TUI*
   rendering a status line — whether it fires inside a headless Cloud
   Routine session (like this one) is still an open, unresolved question,
   flagged explicitly rather than assumed either way. This project's own
   `CLAUDE.md` "no reliable programmatic remaining-quota API" conclusion
   is unchanged pending that answer.

## Files changed

- `backlog/research/claude-code-productivity-tools.md` → `git mv`'d to
  `done/research/claude-code-productivity-tools.md`, `status: done`.
  Rewrote the top-of-file correction note, Findings #0 (headroom/graphify/
  codeburn/ponytail entries), Findings #1 (ccusage entry), and the
  Checkpoint (Latest state / Next steps / Learnings) to reflect this
  pass's verification work.

## Ideas discovered

- The rate-limit-visibility question (item #4 above) is worth a real test
  the next time a *human* runs Claude Code locally with a `statusLine`
  configured and a live session: check whether `rate_limits` actually
  populates, and separately, whether a Cloud Routine session ever invokes
  a statusLine callback at all. If it does turn out to be reachable from
  inside a Routine, that would directly upgrade this project's own
  `CLAUDE.md` "Deferred to v2 — quota polling" position, which currently
  assumes no reliable programmatic option exists. Not verified this pass;
  flagging as a concrete, scoped thing a future pass (or the user
  directly) could check.
- Four adjacent quota/usage tools surfaced via search but weren't vetted:
  `rjwalters/claude-monitor`, `grzegorz-raczek-unit8/claude-quota`,
  `aqua5230/usage`, and a closer look at `tddworks/ClaudeBar`'s own site.
  Left as an unstarted thread in the (now-closed) file's Next steps in
  case a future pass wants to extend the catalogue rather than open a new
  file.

## Open questions

- Does a headless Cloud Routine session ever trigger a `statusLine`
  callback, or is that strictly an interactive-TUI-only code path? This
  is the one genuinely load-bearing unresolved question from this pass —
  it's the difference between "no programmatic quota API exists" and "one
  exists but this project's runtime can't reach it," which are different
  claims with different implications for any future v2 idea revisiting
  quota awareness.

## Suggested next actions for the user

- Nothing time-sensitive. The two research items still `in-progress`
  (`ai-monetization-overnight-automation`, `free-ai-learning-resources`)
  and the `gumroad-venture-factory-template` business item remain the
  highest-value picks for the next run, per the scoring table above.
- If curious, the user could personally check the `statusLine`
  open question above in about a minute locally (configure a trivial
  `statusLine` script that just dumps its stdin JSON, run one prompt, see
  if `rate_limits` shows up) — faster than a future automated pass could
  verify it, since only a real interactive session can test it.
