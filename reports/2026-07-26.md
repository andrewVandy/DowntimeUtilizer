# Run report — 2026-07-26

## Scoring

All backlog items and their ROI scores this run (formula per `CLAUDE.md`,
`* 1.5` continuation bonus applied to `in-progress`):

| id | status | impact×confidence×priority/effort | score |
|---|---|---|---|
| `ai-monetization-overnight-automation` | in-progress | 100/5 = 20 | **30** |
| `claude-code-productivity-tools` | in-progress | 80/4 = 20 | **30** |
| `gumroad-venture-factory-template` | backlog | 100/4 = 25 | 25 |
| `free-ai-learning-resources` | in-progress | 80/5 = 16 | 24 |
| `ai-coding-freelance-portfolio-kit` | backlog | 36/3 = 12 | 12 |
| `github-to-claude-code-launcher-extension` | backlog | 27/3 = 9 | 9 |

Tied exactly at 30 again (as flagged in the 2026-07-22 report). Picked
`claude-code-productivity-tools` this time: the 2026-07-22 report's own
"suggested next actions" flagged it as tied-but-unpicked, and its open next
steps (verify suspicious aggregator star counts, resolve the "headroom"
naming collision) were concrete, bounded, and directly answerable with the
GitHub API tools available in this session — a good fit for a single-pass
close-out rather than open-ended breadth work.

## Tasks touched

Second research pass on `backlog/research/claude-code-productivity-tools.md`.
Closed all four of pass 1's next steps. Did not touch
`ai-monetization-overnight-automation`, `free-ai-learning-resources`, or any
`backlog/business/` item this run.

## Files changed

- `backlog/research/claude-code-productivity-tools.md` — second research
  pass, `updated` bumped to 2026-07-26, still `in-progress` (breadth-first
  mandate, more categories left to re-verify). Key changes:
  - **Resolved the "headroom" naming collision** — found a fifth,
    previously-missed repo (`headroomlabs-ai/headroom`, 62,471★) that
    accounts for the skillsllm.com star figure pass 1 flagged as
    suspicious. It's real, just wasn't in the catalogue yet.
  - **Verified GitHub star counts directly via the GitHub API** (not
    aggregator scraping) for 11 repos — see the table in the file's
    "Findings — pass 2" section. `graphify`'s real count (96,017) is
    *higher* than the aggregator figure pass 1 was skeptical of, not
    lower.
  - **Caught and corrected a real pass-1 error**: the "official-limit
    trust layer" (`rate_limits` + provenance labels) was misattributed to
    `ccusage`. Fetching both projects' READMEs directly showed it actually
    belongs to `Claude-Code-Usage-Monitor`. Corrected in the note above the
    frontmatter and in Findings #1.
  - **New finding, not in pass 1**: three of the file's most-cited repos
    (`graphify`, `ponytail`, `headroomlabs-ai/headroom`) show
    thousands-of-stars-per-week sustained growth since creation — several
    times faster than the two much older, more established comparison
    repos in the same file. Flagged as a "verify before citing as adoption
    evidence" signal, not proof of anything, in the same family as the
    OpenClaw content-farm signal already flagged in the sibling
    monetization research file.
  - Confirmed `ponytail` is genuinely organically viral via an X/Twitter
    pass (independently-worded threads in three languages), the strongest
    organic-adoption signal found in this file so far.
  - Surfaced a small cluster of new usage-monitor tools (AIQuotaBar and
    similar) not individually verified yet — queued as a next step.

Nothing moved to `done/` this run — this is a breadth-first research item
by design (see its own `## Context` section), not scoped to reach `done`.

## Ideas discovered

Nothing new for `backlog/business/` this run — this pass was verification
and correction of existing catalogue entries, not new-territory research.
The star-velocity anomaly (above) could seed a future research angle
("how much of the Claude Code tooling ecosystem's apparent traction is
real vs. inflated") but that's speculative, not concrete enough to file as
its own item yet.

## Open questions

- The star-velocity flag needs follow-up with a mechanism *other* than
  GitHub's own star count to actually settle it (npm/PyPI downloads,
  independent HN/blog discussion volume, star-history anomaly tools) —
  this run named the concern but didn't have a way to verify it further
  within this environment's tools.
- Categories 2, 3, 4, 6, 8, 9, 10 in the productivity-tools file (session
  viewers, git helpers, hooks libraries, MCP memory servers, orchestration,
  IDE integrations, dotfiles) haven't had the same GitHub-API-verification
  treatment this pass gave to categories 0, 1, and 7 — still running on
  pass-1's search-snippet-only sourcing.

## Suggested next actions for the user

1. No action required — the corrected file is ready to read or hand off,
   and both this item and `ai-monetization-overnight-automation` remain
   valid top-scoring picks for a future run (still tied at 30).
2. If you plan to actually install/recommend any of `graphify`, `ponytail`,
   or `headroomlabs-ai/headroom` to yourself or others, read the
   star-velocity caveat in the file's Learnings section first — the tools
   may well be exactly as good as claimed, but their star counts alone
   aren't strong evidence of that given the growth-rate anomaly found this
   pass.
3. Worth deciding whether `claude-code-productivity-tools` should be
   marked `done` on its next pass (closing remaining categories) rather
   than left open-ended indefinitely, since unlike the monetization
   research file it doesn't have as strong a stated reason to stay
   perpetually breadth-first.
