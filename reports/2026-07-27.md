# Run report — 2026-07-27

## Note: significant PR pileup — 4 open, unmerged PRs sitting on `main`

Before picking work, checked open PRs and found four unmerged since
2026-07-23: **#3** (closes out `ai-monetization-overnight-automation`,
seeds 3 business items), **#4** (closes out `claude-code-productivity-tools`,
first close-out), **#5** (ships `gumroad-venture-factory-template` end to
end), and **#6** (a *second*, independent close-out attempt on
`claude-code-productivity-tools` — fully redundant with #4, created two
days later without knowledge #4 existed, because none of this had merged
into `main` yet).

Because none of #3/#4/#5/#6 are merged, `main`'s backlog files still show
`ai-monetization-overnight-automation`, `claude-code-productivity-tools`,
and `gumroad-venture-factory-template` as unfinished/`in-progress` — which
is exactly what this run's own ROI scoring saw (see table below). Picking
any of the top three by score would have produced a *third* duplicate
piece of work on top of what's already sitting in #3/#4/#6. Skipped all
three and worked the next-highest-scoring item genuinely untouched by any
open PR instead: `free-ai-learning-resources`.

**This is now urgent, not just a data point.** Every day these PRs sit
unmerged risks another run repeating this exact waste (a 5th duplicate
next time). Recommended action for the user, in order:

1. **Merge #4** (`claude-code-productivity-tools` close-out) — it's the
   more thorough/accurate of the two attempts on that item per its own
   description (caught a pass-1 misattribution, resolved a naming
   collision pass-1 missed).
2. **Close #6** without merging — its content is superseded by #4 and
   merging both would double-apply conflicting changes to the same file.
   (#6's description is honest about this risk-worth a quick read before
   closing, in case it independently caught something #4 missed.)
3. **Merge #3** (`ai-monetization-overnight-automation` close-out + 3 new
   business items) and **#5** (`gumroad-venture-factory-template` shipped)
   — both look complete and non-conflicting with each other or with #4.
4. Expect merge conflicts between these PRs (all branched from different,
   now-stale points on `main`) — resolve in whatever order is easiest;
   none of the four look intentionally destructive, just diverged.

This run did not attempt to merge or close any of these PRs itself —
`POLICY.md`'s "a human merges" branch policy is a deliberate guardrail
this run is following, not an oversight. It also predates PR #5's own
proposed fix (a POLICY.md rule to check for overlapping PRs before
starting work) — that PR isn't merged yet, so the rule isn't official yet,
but this run applied the same logic anyway since the evidence was sitting
right there.

## Scoring

All backlog items and their ROI scores this run, computed from `main`'s
actual current state (formula per `CLAUDE.md`, `× 1.5` continuation bonus
applied to `in-progress`):

| id | status | impact×confidence×priority/effort | score | covered by open PR? |
|---|---|---|---|---|
| `ai-monetization-overnight-automation` | in-progress | 100/5 = 20 | **30** | yes — #3 |
| `claude-code-productivity-tools` | in-progress | 80/4 = 20 | **30** | yes — #4 and #6 |
| `gumroad-venture-factory-template` | backlog | 100/4 = 25 | 25 | yes — #5 |
| `free-ai-learning-resources` | in-progress | 80/5 = 16 | 24 | no — **picked** |
| `ai-coding-freelance-portfolio-kit` | backlog | 36/3 = 12 | 12 | no |
| `github-to-claude-code-launcher-extension` | backlog | 27/3 = 9 | 9 | no |
| `example-competitor-scan` | backlog | 16/2 = 8 | 8 | no |

Two items tied at the top (30) and a third close behind (25) all already
have completed, unmerged work sitting in PRs. `free-ai-learning-resources`
is the actual highest-scoring item this run could make real (non-
duplicate) progress on.

## Tasks touched

Worked `backlog/research/free-ai-learning-resources.md` — second research
pass, now `status: done`, moved to `done/research/`. First pass (2026-07-21)
covered 7 categories / 40+ resources; this pass closed both gaps it left
open and added substantial new breadth:

- **Reddit access**: retested directly (`WebFetch` on reddit.com,
  `WebSearch` with `allowed_domains:["reddit.com"]`) — confirmed a hard
  tool-level block, not a search-indexing gap, matching the sibling
  monetization file's finding exactly. Closed permanently; future passes
  shouldn't retry.
- **Math prerequisites** (new section 13, 12 resources): linear algebra,
  calculus, probability/statistics, optimization — courses (MIT OCW
  18.01/18.02/6.041/18.05, Stanford EE364A), interactive tools (Immersive
  Math, Seeing Theory), and books (Mathematics for Machine Learning,
  Khan Academy, Paul's Online Math Notes).
- **5 new categories** (sections 8-12, ~25 resources): agent-framework-
  specific courses (LangChain Academy, CrewAI, AutoGen), AI safety/
  alignment beyond Anthropic's own (BlueDot Impact, ARENA, Center for AI
  Safety, Tübingen/Harvard courses), cloud-provider free AI learning paths
  (Google Skills, AWS Skill Builder, NVIDIA DLI), curated GitHub
  "awesome-list" repos, and other modalities (paper-reading communities,
  competitive-learning platforms beyond Kaggle).
- New finds via X/Twitter search (which remains searchable, unlike
  Reddit): MIT 18.06 Linear Algebra, NYU's deep learning course
  (LeCun/Canziani), MIT Open Learning's curated free-book collection,
  Serrano.Academy YouTube channel.

File now has 13 categories, 100+ distinct resources, every category the
user explicitly requested is covered, plus substantial bonus breadth.
Judged sufficiently exhaustive — moved to `done` rather than spinning a
third pass on diminishing returns.

Spun one new backlog item per the file's own flagged next step: converting
the flat catalogue into an ordered beginner→advanced learning path is a
distinct synthesis task, not more cataloguing.

## Files changed

- `done/research/free-ai-learning-resources.md` (moved from
  `backlog/research/`, `status: done`) — second research pass, checkpoint
  rewritten, `updated` bumped to 2026-07-27.
- `backlog/documentation/ai-learning-path-guide.md` (new) — ordered
  learning-path guide, `depends_on: [free-ai-learning-resources]`,
  scored `impact 4 / confidence 4 / priority 3 / effort 3` (score 16
  once eligible).
- `reports/2026-07-27.md` (this file).

## Ideas discovered

- The PR-pileup pattern flagged in PR #5's description (2026-07-25) is
  not hypothetical — it's actively happening (#6 duplicating #4). Worth
  the user prioritizing a merge pass over letting more nightly runs
  accumulate on top of a stale `main`.
- Once `ai-learning-path-guide` is workable (after `free-ai-learning-
  resources` is confirmed merged to `main`), it's a clean, bounded
  synthesis task — good candidate for a future run with a tight time
  budget, since it needs no new research, just restructuring already-
  verified material.

## Open questions

- Should the routine itself ever be allowed to close a PR it can prove is
  fully superseded by another (like #6 vs #4), even though `POLICY.md`
  says "a human merges"? This run erred toward doing nothing rather than
  guessing at an expanded mandate — flagging the question rather than
  deciding it unilaterally.
- Two math-prerequisite sources (`mml-book.github.io`, `ocw.mit.edu`) and
  five sources in the new sections 9-10 (bluedot.org, skills.google,
  skillbuilder.aws, nvidia.com, aisafetybook.com) returned HTTP 403 to
  direct `WebFetch` in this environment and were corroborated via search
  snippets only, not primary-source fetch. Low risk (multiple independent
  snippets agreed), but worth a spot-check if anyone relies on the exact
  figures cited for those entries.

## Suggested next actions for the user

1. **Merge the PR backlog in the order above** (#4 → close #6 → #3 → #5)
   — this is the single highest-leverage thing to do before the next
   nightly run, or the duplicate-work problem will keep compounding.
2. Review and merge this run's PR (`free-ai-learning-resources` → done,
   plus the new `ai-learning-path-guide` documentation item).
3. No action needed on `free-ai-learning-resources` itself — it's a
   complete reference asset as-is; the learning-path guide is optional
   polish, not a blocker.
