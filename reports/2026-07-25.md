# Run report — 2026-07-25

PR for this run: https://github.com/andrewVandy/DowntimeUtilizer/pull/5

## Read this first: two PRs from prior runs are still unmerged

Before any other content in this report: **PR #3** (opened 2026-07-23) and
**PR #4** (opened 2026-07-24) are both still open, unreviewed, unmerged.
Between them they already close out both items that tied for the top ROI
score this run (`ai-monetization-overnight-automation`,
`claude-code-productivity-tools`), plus add three new `backlog/business/`
items. Because they haven't merged, `main` — and therefore this run's
scoring — still treated both of those items as `in-progress`, not `done`.

This run's first attempt was a second research pass on
`claude-code-productivity-tools` (the tied-top item this run picked,
same as the last run's stated intent). Partway through, a check of open
PRs against the repo turned up PR #4, which had **already done the same
pass, reached a more accurate conclusion** (it found a fifth "headroom"
GitHub repo, `headroomlabs-ai/headroom` at ~62k stars, that this run's own
pass missed — which meant this run's conclusion that `skillsllm.com`'s
star counts were "badly wrong" was itself wrong, just from an incomplete
search). That work was discarded rather than pushed as a competing,
lower-quality duplicate. See Tasks touched below for what this run did
instead, and see the new rule added to the shipped `POLICY.md` template
(Files changed) — this exact situation is now a documented lesson in the
product this run ended up shipping.

**Recommended action**: review and merge (or close) PR #3 and PR #4 soon.
Every day they sit open, the routine's scoring is working off a stale
view of what's actually done, which risks this exact wasted-duplicate-work
pattern recurring.

## Scoring

All backlog items and their ROI scores this run, as computed from `main`
(formula per `CLAUDE.md`, `* 1.5` continuation bonus applied to
`in-progress`) — note this table reflects `main`'s state, which is stale
relative to PR #3/#4 as explained above:

| id | status (on `main`) | impact×confidence×priority/effort | score |
|---|---|---|---|
| `ai-monetization-overnight-automation` | in-progress | 100/5 = 20 | **30** |
| `claude-code-productivity-tools` | in-progress | 80/4 = 20 | **30** |
| `gumroad-venture-factory-template` | backlog | 100/4 = 25 | 25 |
| `free-ai-learning-resources` | in-progress | 80/5 = 16 | 24 |
| `ai-coding-freelance-portfolio-kit` | backlog | 36/3 = 12 | 12 |
| `github-to-claude-code-launcher-extension` | backlog | 27/3 = 9 | 9 |
| `example-competitor-scan` | backlog | 16/2 = 8 | 8 |

The two tied-at-30 items are both already substantively finished in the
unmerged PRs described above. `gumroad-venture-factory-template` (score
25) is the highest-scoring item genuinely untouched by any open PR, and
this project's actual first revenue attempt — picked it for this run's
real work after discarding the duplicate `claude-code-productivity-tools`
attempt.

## Tasks touched

**Discarded** (not pushed): a second research pass on
`claude-code-productivity-tools`, once PR #4 was found to have already
done the same work more accurately. No file changes from this attempt
made it into the final commit.

**Completed**: `backlog/business/gumroad-venture-factory-template.md`,
end to end, through everything the routine can do without spending real
money or creating real accounts. Moved to `done/business/`.

## Files changed

- `done/business/gumroad-venture-factory-template.md` (moved from
  `backlog/business/`, `status: done`) — rewrote Checkpoint.
- `done/business/gumroad-venture-factory-template/` (new directory,
  moved from `backlog/business/`) — the actual deliverable:
  - `template/` — a full sanitized, generic starter repo: `README.md`,
    `CLAUDE.md`, `POLICY.md`, `ROUTINE_PROMPT.md`, `backlog/_template.md`,
    one worked example project, and empty `done/`/`reports/` scaffolding.
    Grepped clean for this project's own name/owner/email — no leakage.
    **`POLICY.md` includes one addition sourced directly from this run's
    own experience**: a rule to check for duplicate/overlapping unmerged
    PRs before starting work on the top-scored item, so a buyer's own
    setup doesn't repeat today's wasted-effort pattern.
  - `GUIDE.md` — buyer-facing guide covering the four core design ideas
    (files as memory, the scoring formula and why the finish-bonus
    matters, checkpoints vs. logs, human-merges-only) and what a
    reasonable first month looks like.
  - `LISTING.md` — draft Gumroad title/tagline/description/price ($24,
    anchored against the one directly-comparable priced competitor found
    in the item's original differentiation check) and a cover-image
    brief. Flags two things for the user to fill in before publishing: a
    real run-count number, and a quick re-check that no new direct
    competitor has appeared.
  - `DISTRIBUTION.md` — closes a gap flagged by the monetization research
    (category #15: output without a distribution plan is the most common
    way this kind of project earns $0): a real channel plan leading with
    a build-in-public post about the underlying project, not a direct
    sales pitch — specific channels (Show HN, Indie Hackers, a relevant
    subreddit, `awesome-claude-code` submission) reasoned from what that
    research actually found converts in this space.
- `reports/2026-07-25.md` (this file).

Nothing else on `main` was touched this run.

## Ideas discovered

- The PR-pileup issue (see top of report) is itself the most important
  thing this run found — worth treating as a process issue for the
  project, not just a one-off annoyance.
- Writing the Gumroad template surfaced that this project's own
  `POLICY.md` was missing a guardrail this run needed in real time (check
  for overlapping unmerged PRs before starting). Only added it to the
  *shipped template's* `POLICY.md` so far — worth considering whether
  this repo's own `POLICY.md` should get the same addition, since the
  exact scenario it guards against just happened here.

## Open questions

- Should this repo's own `POLICY.md` adopt the same "check for
  overlapping unmerged PRs" rule added to the shipped template? It would
  have prevented this run's wasted first attempt.
- `LISTING.md`'s description has a `[N]` placeholder for a real run-count
  number — needs a genuine figure before publishing, not a guess.
- Worth deciding whether to review/merge PR #3 and PR #4 as-is, or ask a
  future run to reconcile them (e.g. re-verify PR #4's star-count claims
  once more, since this run independently confirmed its
  `headroomlabs-ai/headroom` finding but didn't cross-check every other
  number in that PR).

## Suggested next actions for the user

1. **Highest priority**: review and merge or close PR #3 and PR #4 — both
   are two-plus days old and represent real, mostly-solid finished work
   sitting idle, and their being unmerged is what caused this run's
   wasted first attempt.
2. Read `done/business/gumroad-venture-factory-template/GUIDE.md` and
   `LISTING.md`, fill in the run-count placeholder once you have a real
   number, and decide whether you want to actually create the Gumroad
   seller account and publish — that step is deliberately left manual.
3. Consider adding the "check for overlapping PRs" guardrail to this
   repo's own `POLICY.md` (see Open questions) so this exact failure mode
   doesn't recur on a future run.
