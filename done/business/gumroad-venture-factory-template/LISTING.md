# Gumroad listing copy — draft

## Product name

**Backlog Autopilot: A Claude Code Nightly Ops Kit**

(Working title — swap in whatever you're comfortable putting your name
behind. Alternatives considered: "The Nightly Backlog Kit," "Idle Quota
Autopilot," "Claude Code Overnight Ops." Went with "Backlog Autopilot"
because it describes the mechanism in two words rather than the benefit —
matches how the strongest comparable listing, "Claude Code Project
Starter Pack," names itself by what's inside, not by a promise.)

## Tagline (under the title)

Turn your unused Claude Code quota into shipped work, every night —
a working template for a self-checkpointing, autonomously-scored backlog
system, not another prompt list.

## Price

**$24.** Reasoning: the one directly-comparable priced product found in
the differentiation check (`Claude Code Migration Playbook`, Yurukusa) is
$19 for a single-topic playbook. This product includes a complete working
system (backlog schema, ROI-scoring engine as a prompt convention,
guardrail policy, routine prompt, and a buyer's guide explaining the
reasoning) rather than a single-topic guide or a prompt list — priced
modestly above the $19 anchor to reflect more total substance, while
staying under $29 so it doesn't need to compete on the "comprehensive
manual" axis (the 91-page *Complete Claude Code Playbook* is positioned
there). Re-check actual current prices on the comparable listings before
publishing — this project's own tools can't verify exact current Gumroad
prices with certainty; treat $24 as a starting anchor to confirm, not a
final number.

## Description

```
Your Claude subscription has quota sitting idle every night. This is a
working template for turning it into shipped work — a Cloud Routine that
wakes up on a schedule, reads a backlog you wrote, works the single
highest-value item it can find, and opens a PR for you to review. No
babysitting required.

This isn't a prompt pack. It's the actual operating system for an
autonomous backlog: a scoring formula that prevents an agent from
starting twenty things and finishing none, a checkpoint convention so a
stateless agent with no memory of yesterday can pick up exactly where it
left off, and a guardrails file that keeps an unattended agent from doing
anything risky — no real money spent, no messages sent to anyone, nothing
touching production — until you explicitly say otherwise.

What's included:

  - A ready-to-push template repo: backlog schema, ROI-scoring
    convention, guardrails policy, the exact routine prompt to paste into
    Claude Code's /schedule, and a worked example project file
  - A buyer's guide explaining *why* it's built this way — not just the
    mechanics, but the reasoning behind every design decision, so you can
    adapt it intelligently instead of copying blindly
  - Setup instructions for the one-time manual steps (GitHub push,
    connecting Claude Code, creating the schedule) — roughly 15 minutes
    start to finish

Built by actually running this exact system for [N] real overnight runs
on my own backlog before productizing it — this is the same thing I use,
not a theoretical writeup.

Requires a Claude subscription with Claude Code access. No coding
experience required to set up; basic comfort with git and the command
line will make the 15-minute setup faster.
```

(Fill in `[N]` with your own real run count before publishing — do not
publish a fabricated number. If you're publishing this before you've run
it yourself for a while, cut that sentence rather than guess a number.)

## Cover image brief

A clean, screenshot-style graphic (not a stock photo) showing:
- Left: a simplified "backlog" list — three or four project titles with
  small colored score badges next to them (evokes the scoring mechanic
  visually without needing text to explain it).
- Right: a small "PR opened" card / GitHub-style diff snippet, implying
  the output is real, mergeable work, not a chat transcript.
- A single line of text overlay: "Idle quota → shipped work, every
  night."
- Keep the palette to 2-3 colors, high contrast, legible as a thumbnail
  (Gumroad search results render this small) — avoid dense text.

## Tags / category

Software Development → Templates. Suggested tags: `claude-code`,
`automation`, `productivity`, `developer-tools`, `ai-agents`, `template`.

## Before publishing — re-run the differentiation check

The differentiation check in this item's own backlog file (done
2026-07-21) found the "generic prompt pack" niche saturated but no
existing product covering *this specific mechanism* (an actual
Cloud-Routine-driven, ROI-scored, checkpointed backlog system). Re-search
Gumroad for "claude code backlog," "claude code routine," and "claude
code autonomous agent" listings before publishing, in case that's changed
since the check was done — a fast few-minutes search, not a full research
pass.
